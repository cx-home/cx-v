# std-lib conformance coverage — SUPERSEDED

This hand-shaped manifest is **no longer generated** and was stale. The live,
regenerated coverage artifacts are:

- **Report (human):** [`docs/stdlib-coverage.md`](../../docs/stdlib-coverage.md)
- **Data (machine / guide source):** [`coverage.cx`](coverage.cx)

Regenerate both with `make stdlib-coverage` (runs
`scripts/stdlib_coverage.py`). Do not hand-edit the generated files.
